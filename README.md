# Otis

File-based HTTP client. Desktop app built with Wails v3, React, TypeScript,
Tailwind v4, shadcn/ui and TanStack Router.

See `CLAUDE.md` for project constraints and layout.

```bash
wails3 dev     # development
wails3 build   # production binary in bin/
```
